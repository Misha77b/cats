import { 
    
} from "./breeds.actions";

const initState = {
    breeds: [],
}

const breedsReducer = (state = initState, action) => {
    switch(action.type) {


        default:
            return state
    }
}

export default breedsReducer