import { 
    
} from "./gallary.actions";

const initState = {
    gallary: [],
}

const gallaryReducer = (state = initState, action) => {
    switch(action.type) {


        default:
            return state
    }
}

export default gallaryReducer